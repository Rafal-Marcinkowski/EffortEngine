<mah:MetroContentControl 
             x:Class="EffortEngine.MVVM.Views.InformationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EffortEngine.MVVM.Views"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:Conv="clr-namespace:SharedProject.Converters;assembly=SharedProject"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border 
                BorderBrush="DimGray"
                BorderThickness="3">
            
            <StackPanel VerticalAlignment="Center" Grid.Column="0" HorizontalAlignment="Center">
            
                
                <StackPanel>
                    
                    <TextBlock
                    Text="Ostatnia akcja"
                    Height="auto"
                    Foreground="DimGray"
                    FontSize="24"
                    Style="{StaticResource TextBlockStyle}"/>
                    
                    <TextBlock
                    Text="{Binding RecentActionText, UpdateSourceTrigger=PropertyChanged}"
                    Foreground="DimGray"
                    Style="{StaticResource TextBlockStyle}"
                    FontSize="18"/>
                    
                </StackPanel>
                
        </StackPanel>
            </Border>
    </Grid>
    
</mah:MetroContentControl>
