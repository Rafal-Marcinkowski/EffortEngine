<mah:MetroContentControl 
    
             x:Class="EffortEngine.MVVM.Views.ManageTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EffortEngine.MVVM.Views"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:Conv="clr-namespace:SharedProject.Converters;assembly=SharedProject"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <ContentControl Grid.Column="0" prism:RegionManager.RegionName="TaskTableRegion"/>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Szczegóły zadania" FontWeight="Bold" FontSize="18"/>
            <TextBox Text="{Binding SelectedTask.Description, Mode=TwoWay}" AcceptsReturn="True" Height="100"/>
            
            <Button Content="Zapisz zmiany" Command="{Binding SaveTaskCommand}" />
            <Button Content="Usuń zadanie" Command="{Binding DeleteTaskCommand}" />
        </StackPanel>

        <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="10">
            <TextBlock Text="Sesja Pomodoro" FontWeight="Bold" FontSize="18"/>
            <TextBlock Text="{Binding TimerDisplay}" FontSize="36" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Start" Command="{Binding StartPomodoroCommand}" />
                <Button Content="Pause" Command="{Binding PausePomodoroCommand}" />
                <Button Content="Reset" Command="{Binding ResetPomodoroCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>


</mah:MetroContentControl>
