<mah:MetroContentControl 
             x:Class="EffortEngine.MVVM.Views.AddProgramProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EffortEngine.MVVM.Views"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:Conv="clr-namespace:SharedProject.Converters;assembly=SharedProject"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <DockPanel Margin="25">

            <TextBox 
                x:Name="ProgramProjectNameTextBox"
               Style="{StaticResource TextBoxStyle}"
                MinWidth="450"
                Controls:TextBoxHelper.Watermark="Nazwa programu/projektu"/>

            <Button
                Style="{StaticResource BigButtonStyle}"
                Command="{Binding AddProgrammingTaskCommand}"
                Width="100">
                <iconPacks:PackIconMaterial Kind="ContentSave" Width="30" Height="30"/>
            </Button>

        </DockPanel>
    </Grid>
    
</mah:MetroContentControl>
